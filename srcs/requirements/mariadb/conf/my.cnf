# ------------------------------------------------------------------------------
# Purpose: Minimal MariaDB config for Docker + WordPress
#
# Notes:
# - bind-address=0.0.0.0 so other containers on the network can connect.
# - skip-name-resolve avoids DNS reverse lookups (faster, fewer surprises).
# - Buffer/connection limits are tuned for small VMs.
# - You can optionally enable utf8mb4 defaults (see commented lines).
# ------------------------------------------------------------------------------

[mysqld]
datadir=/var/lib/mysql                     # where data lives (mounted volume)
socket=/run/mysqld/mysqld.sock             # local socket path
bind-address=0.0.0.0                       # listen on all interfaces (container)
pid-file=/run/mysqld/mysqld.pid            # pid file location

# Safer defaults for small VM
skip-name-resolve                           # don't do DNS lookups for clients
innodb_buffer_pool_size=128M                # moderate RAM footprint
max_connections=50                          # cap concurrent client sessions

# (Optional) Modern UTF-8 defaults (uncomment to enforce at server level)
# character-set-server = utf8mb4
# collation-server     = utf8mb4_unicode_520_ci
